{
  "name": "quatrace-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "QuaTrace end-to-end and accessibility tests (Playwright + axe)",
  "scripts": {
    "test": "playwright test",
    "test:a11y": "playwright test --grep @a11y"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.46.1"
  }
}
